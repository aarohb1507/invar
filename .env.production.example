# Example production environment variables
# Do NOT commit secrets to the repository. Use a secret manager or set these on your host.

# PostgreSQL connection (use 'postgres' service name inside docker-compose)
POSTGRES_USER=invar
POSTGRES_PASSWORD=invar_prod_password_change_me
POSTGRES_DB=invar
DATABASE_URL=postgresql://invar:invar_prod_password_change_me@postgres:5432/invar

# Redis connection (use 'redis' service name inside docker-compose)
REDIS_URL=redis://redis:6379

# Server port
PORT=3000

# Environment
NODE_ENV=production

# API key for ingest endpoint
# Generate a new one: openssl rand -base64 32
INVAR_API_KEY=your_production_api_key_here_generate_new_one
